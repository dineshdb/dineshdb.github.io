---
import Nav from "./Nav.astro";

const { current = "" } = Astro.props;
---

<header class="sticky top-0 z-50 w-full bg-white/80 dark:bg-stone-900/80 backdrop-blur-md border-b border-stone-200 dark:border-stone-800">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <Nav current={current} />
  </div>
</header>
